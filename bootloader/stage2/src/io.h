#pragma once

/**
 * @brief Write the string `str` and a trailing new line to the TTY
 *
 * @param str Null terminated string
 */
void puts(char *str);

/**
 * @brief Write the character `ch` to the TTY
 *
 * @param ch The character to write
 */
void putc(char ch);
